# Archive Index

Append-only finding aid for deleted cascade artifacts. One row per artifact:
`artifact → where its conclusion migrated → recovery tag`. Git is the archive;
the repo is lean. To recover a file's pre-deletion form:

```bash
git show <tag>:<artifact>
```

| Artifact | Conclusion migrated to | Recovery tag |
|----------|-----------------------|-------------|
| `specs/reactive-delegation.md` | `specs/supervisor-decision.md` (SD-1…SD-21 re-own the surviving contracts; flag/budget/delegated-mode removals encoded as positive rules) | `docs-archive-2026-06-29` |
| `plans/impl/reactive-delegation.md` | Implemented — conclusion absorbed into `specs/supervisor-decision.md`; design rationale retained in code comments where code-alone-would-mislead | `docs-archive-2026-06-29` |
| `plans/impl/cascade-control.md` | Superseded by `plans/impl/realign-training-roles.md` (T4) — its reactive-round budget and silent-drop behaviour removed under `specs/supervisor-decision.md` SD-3 | `docs-archive-2026-06-29` |
| `specs/signature.md` | Source-is-truth: expressed by `src/kalvin/kline.py`, `src/kalvin/signifier.py`, `src/kalvin/abstract.py`; domain term retained in `CONTEXT.md` (Signature) | `source-is-truth-2026-08-06` |
| `specs/tokenizer.md` | Source-is-truth: expressed by `src/kalvin/abstract.py` (`KTokenizer`), `src/kalvin/tokenizer.py` (`Tokenizer` BPE wrapper) | `source-is-truth-2026-08-06` |
| `specs/nlp_tokenizer.md` | Source-is-truth: expressed by `src/kalvin/nlp_tokenizer.py` (`NLPTokenizer`, node layout, type dictionary, POS_X fallback) | `source-is-truth-2026-08-06` |
| `specs/kline.md` | Source-is-truth: expressed by `src/kalvin/kline.py` (`KLine`, structural predicates, equality); structural-kind terms in `CONTEXT.md` (Structure) | `source-is-truth-2026-08-06` |
| `specs/kvalue.md` | Source-is-truth: expressed by `src/kalvin/kvalue.py` (`KValue`, structural equality) and `src/kalvin/events.py` (`RationaliseEvent`); term in `CONTEXT.md` (KValue) | `source-is-truth-2026-08-06` |
| `specs/stm.md` | Source-is-truth: expressed by `src/kalvin/stm.py` (`STM` — bounded dual-keyed index, FIFO eviction, thread safety) | `source-is-truth-2026-08-06` |
| `specs/model.md` | Source-is-truth: expressed by `src/kalvin/model.py` (four-tier Model, write cascade, traversal, `is_countersigned`, `grounded`), `src/kalvin/significance.py` (8-bit algebra, `BandLayout`, sentinels, `Aggregator`), `src/kalvin/expand.py` (`expand`, `edge_hops`), `src/kalvin/proposals.py` (`generate_expansions`, misfit comprehension). Spec's standalone `is_s1` was an abstraction the code materialises via `model.grounded()` + structural predicates — naming divergence only, no behaviour gap. | `source-is-truth-2026-08-06` |
| `specs/cogitator.md` | Source-is-truth: expressed by `src/kalvin/cogitator.py` (`Cogitator`, `WorkItem`, `CogitationHandler`, drain contract) consuming `expand.py`/`proposals.py`; vision owns the S2-expansion concept | `source-is-truth-2026-08-06` |
| `specs/rationaliser.md` | Source-is-truth: expressed by `src/kalvin/rationaliser.py` (`Rationaliser` — pipeline Phases 1–5, routing, `_promote_participating`, `on_s1`/`on_expansion`, `countersign`, serialization delegation); term in `CONTEXT.md` (Cogitation/Rationalisation). Spec test-row AGT-7 (assign-sig-if-missing) was stale vs the spec body and the code's `assert` — code is the truth. | `source-is-truth-2026-08-06` |
| `specs/kscript.md` | Source-is-truth: expressed by `src/ks/` — `token.py`/`lexer.py` (tokens, lexing), `ast.py`/`parser.py` (scope-model AST), `binding_scope.py` (Word Binding B1–B4), `ast_emitter.py` (scope rules, MTS §8, subscript identity), `token_encoder.py` (encoding, §11.3 compound-word, §11.4 canonical registry, §11.6 source-before-decomposition), `compiler.py`/`__init__.py` (`KScript` API); terms in `CONTEXT.md` (KScript, Relational Tokens, MTS, Word Binding, Target Significance) | `source-is-truth-2026-08-06` |
| `specs/curriculum.md` | Source-is-truth: expressed by `src/training/trainer/curriculum_document.py` (markdown parse/validate/amend), `curriculum.py` (`Curriculum`/`CurriculumState`, label + entry tracking, persistence), `curriculum_generator.py` (LLM goal→curriculum); term in `CONTEXT.md` (Curriculum) | `source-is-truth-2026-08-06` |
| `specs/trainer-satisfaction.md` | Source-is-truth: expressed by `src/training/trainer/trainer.py` (satisfaction = `satisfied ⊇ submitted` at declared band, lesson-completion guard, supervisor escalation) and `reactor.py` (auto-countersign, recurrence dedup). Spec described a transitional "paced-loop" partition/Held-Index design its own header marked superseded; the production code uses the Reactor + supervisor-decision path instead — code is the truth. | `source-is-truth-2026-08-06` |
| `specs/training-log.md` | Source-is-truth: expressed by the `logging` calls in `src/training/trainer/trainer.py`, `reactor.py`, and `src/training/harness/adapter.py` (session/lesson/event/satisfaction log lines, decompiled via `kline_display`); auto-tune capture in `src/training/auto_tune/` | `source-is-truth-2026-08-06` |
| `specs/harness-server.md` | Source-is-truth: expressed by `src/training/harness/` — `bus.py` (role-based `MessageBus`, fan-out, wildcard, error replies), `message.py`/`constants.py`, `adapter.py` (Kalvin adapter: submit/countersign/rationalise, sender map, three payload forms), `server.py` (config-driven participant loading, WebSocket, bus loop), `protocol.py` (registration, JSON frames, silent-drop), `llm.py`; and `src/training/supervisors/` — `commands.py` (shared command parser), `tui_client.py`/`slack_agent.py`/`cli_supervisor.py`/`llm_supervisor.py`. Operator guide retained at `src/training/harness/README.md`. | `source-is-truth-2026-08-06` |
| `specs/supervisor-decision.md` | Source-is-truth: expressed by the decision-gate logic in `src/training/trainer/trainer.py` (decision request/answer, hold-and-replay gate, lesson-boundary drain window, ratify/scaffold/continue answers) and the `LLMSupervisor` pipeline in `src/training/supervisors/llm_supervisor.py`. Spec's header marked it superseded by the dialogue path but still the running production path; code is the truth. | `source-is-truth-2026-08-06` |
| `specs/dialogue-driven-training.md` | Source-is-truth: expressed by `src/dialogue/` — `decoder.py` (`DialogueScript`/`Turn`/`DecodedTurn`, `decode`, priors-as-runs), `actors.py` (`EventSink`/`Actor`, `ScriptTrainer`/`ScriptTrainee`, `SynthesizingTrainer`, `RationalisingTrainee`/`RationalisingTrainer`), `runner.py` (coverage-tracking wildcard subscriber, terminal conditions, divergence, grounding verification), `synthesize.py`. Spec self-declared a "working sketch, not a frozen contract" — code is the truth. | `source-is-truth-2026-08-06` |
| `specs/dialogue-cogitation.md` | Source-is-truth: expressed by `src/dialogue/rationalise.py` (the rationalising engine: two channels, S3 countersignature path, S2 similar-fit-proposal path, work-list, frame, state injection). Spec self-declared the most speculative part of the sub-project; code is the truth. | `source-is-truth-2026-08-06` |
